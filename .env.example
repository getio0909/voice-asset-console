# Public browser path. Keep this same-origin for HttpOnly session cookies.
VITE_API_BASE_URL=/api/v1

# Vite development/preview proxy only; this value is not bundled into browser code.
VOICEASSET_API_PROXY_TARGET=http://127.0.0.1:8080
